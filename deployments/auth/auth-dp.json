{
    "apiVersion": "extensions/v1beta1", 
    "kind": "Deployment", 
    "metadata": {
        "namespace": "{{.NAMESPACE}}", 
        "name": "auth"
    },
    "spec": {
        "replicas": {{.AUTH_REPLICAS}},
        "template": {
            "spec": {
                "containers": [
                    {
                        "image": "{{$.DOCKER_REPO}}/auth:latest", 
                        "name": "auth", 
                        "ports": [
                            { "containerPort": 8080 }
                        ]
                    }
                ]
            }, 
            "metadata": {
                "labels": {
                    "auth": "true"
                }
            }
        }, 
        "selector": {
            "matchLabels": {
                "auth": "true"
            }
        }
    }
}
