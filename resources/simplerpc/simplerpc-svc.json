{
    "apiVersion": "v1", 
    "kind": "Service", 
    "metadata": {
        "namespace": "{{$.NAMESPACE}}", 
        "name": "simplerpc"
    },
    "spec": {
        "type": "ClusterIP", 
        "ports": [
            {
                "name": "http", 
                "protocol": "TCP", 
                "targetPort": 8080,
                "port": 80
            }
        ], 
        "selector": {
            "simplerpc": "true"
        }
    }
}
