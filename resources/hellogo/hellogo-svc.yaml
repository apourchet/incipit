apiVersion: v1
kind: Service
metadata:
  name: hellogo
  namespace: {{.NAMESPACE}}
spec:
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  selector:
    k8s-app: hellogo
  type: ClusterIP
