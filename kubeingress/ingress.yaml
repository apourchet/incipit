apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: main-ingress
  namespace: dummy
spec:
  backend:
    serviceName: hellogo
    servicePort: 8080
  rules:
  - http:
      paths:
      - path: /hellogo
        backend:
          serviceName: hellogo
          servicePort: 8080
